{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "fill": {
      "description": "本层的内电层填充列表：每一项是一块填充多边形（字段表同 TPcbFill，另多一个 `id`）。\r\n\r\n ⚠️ **盘上每一项仍然带 `layerId` 与 `fillStyle`**：写盘的 key 表里就有这两个键\r\n （编码 keys 含 `layerId` 与 `fillStyle`），值取自该填充自身的所属层与其类型\r\n （内电层填充恒为 `\"PLANE\"`）。\r\n 类型上的 `StrictOmit` 只是**输入侧的写法**（喂数据时这两项由所属层统一决定、不必逐项填），\r\n **不要据此认为盘上没有它们**。",
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  },
  "required": [
    "fill"
  ],
  "description": "层填充"
}