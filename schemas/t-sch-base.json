{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "partId": {
      "description": "所属部件编号（**可选**，真机存在整键缺失的 COMPONENT）：\r\n **符号页**上表示自身归属于哪个部件，**图页**的元件则指向它**关联的符号文档**里的部件。\r\n 取值即目标 `PART` 行的 `id`（`PART` 行只存在于 SYMBOL 文档，见 `TPart`）。",
      "type": "string"
    },
    "groupId": {
      "description": "分组编号；**两种「未成组」形态都表示未成组**：\r\n - **3.0 写盘时该键根本不存在**：写入端取 `model.combination?.id`，未成组即 `undefined`，\r\n   经 `JSON.stringify` 后整键被丢弃，消费方须**按可选键处理**；\r\n - **`\"\"` 只出现在 2.0 迁移数据里**（2.0 → 3.0 转换腿原样带着它）。",
      "type": "string"
    },
    "locked": {
      "description": "是否锁定",
      "type": "boolean"
    },
    "zIndex": {
      "description": "同层内的叠放次序：渲染排序键，数值越小越靠下（格式里没有高度语义）",
      "type": [
        "number",
        "null"
      ]
    },
    "yAxisDirection": {
      "description": "Y 轴方向标记：**仅 eprj3 本地文件格式会带**，读盘时被剥离。\r\n\r\n 取值：`up` **已按 Y 轴向上写出**（本地文件固定用它）、`down` Y 轴向下\r\n （**字段缺失等价于它**，两者是同一件事）。完整语义见 `TYAxisDirection`。\r\n\r\n ⚠️ 本基类被多个图元继承，**各自被翻转的字段不同**——完整对照表见 `TYAxisDirection`。\r\n 本基类直接覆盖的：`ATTR`/`TEXT`/`PIN`/`COMPONENT` 翻 `y`；`OBJ`/`TABLE` 翻 `startY`；\r\n `ARC` 翻 `startY`/`referY`/`endY`；`CIRCLE`/`ELLIPSE` 翻 `centerY` + 内嵌 `text.y`；\r\n `RECT` 翻 `dotY1`/`dotY2` + `text.y`；`MASK_REGION` 翻 `dotY1`/`dotY2`；`POLY` 翻\r\n `points` 各项的 `y`；`BEZIER` 翻 `controls` 的奇数下标。\r\n `rotation` / `isMirror` / `zIndex` 与所有幅值字段**不翻**。",
      "type": "string",
      "enum": [
        "up",
        "down"
      ]
    }
  },
  "required": [
    "groupId",
    "locked",
    "zIndex"
  ],
  "description": "基础字段（原理图系列所有图元的公共前缀）\r\n\r\n【通用约定】下列约定适用于本文件（SCH_PAGE / SYMBOL / SIMULATION）**全部**以本类型为基础的图元：\r\n- **坐标与长度的单位是 0.01 inch**（即 1 个单位 = 10 mil = 0.254 mm）；字号（fontSize）同单位。\r\n- **角度单位是度。读写腿不做任何度↔弧度换算；只有渲染层才转弧度。**\r\n- 每条数据是一行，形如 `{\"type\":..., \"ticket\":..., \"id\":...}||{...}`：前半段是通用外壳\r\n  （`type` 取 `ESchDataType` 的值，`ticket` 是逻辑时钟），后半段才是各类型描述的内容。\r\n  行终止符是 `|` 加换行（**LF，不是 CRLF**）：`||` 是外壳/载荷分界，行尾的 `|`+换行才是记录结束。\r\n  ⚠️ **外壳的键序是 type → ticket → id**（编码器 `ProConsistencyManager` 的\r\n  `const typeObj = { type: dataType, ticket: ticket, id }`；真机每行都是\r\n  `{\"type\":\"ATTR\",\"ticket\":2,\"id\":\"...\"}`），示例照此顺序写。\r\n- **外壳还可能带 `firstTicket` / `pend` / `num` / `client`**（见 `ProConsistencyManager` 的\r\n  `getAtomDataStr`）：`firstTicket` 是首次创建的逻辑时钟，其余是框架附加信息。\r\n  它们**透传即可**，不要当坏数据。\r\n- **`id` 是随机 16 位小写十六进制**（`getRandomUuid()` 生成），文档内唯一。\r\n  ⚠️ **这只是「新建图元」的通则**，存量数据例外很多：`CANVAS` / `NG_SETTING` / `META`\r\n  用**固定串**、`PART` 行的 `id` **就是 `partId`**（如 `-1`、`TPS16417DRCR.1`）、\r\n  2.0 迁移数据的 id 是 `ie0` / `ie1` / `e3` 这类**短串**。\r\n  **不要拿「16 位 hex」去校验存量数据**。"
}