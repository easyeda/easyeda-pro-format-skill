{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "on": {
      "description": "是否启用（不启用则使用 V-CUT）",
      "type": "boolean"
    },
    "stampHoleGroupQuantity": {
      "description": "邮票孔组数",
      "type": "number"
    },
    "stampHoleDiameter": {
      "description": "邮票孔直径（mil）",
      "type": "number"
    },
    "stampHoleQuantityPerGroup": {
      "description": "邮票孔每组数量",
      "type": "number"
    },
    "stampHoleSpacing": {
      "description": "邮票孔间距（mil）",
      "type": "number"
    },
    "center_percent": {
      "description": "邮票孔组位置与板子左侧的距离——**比例值（0~1 的小数，不是百分数）**，\r\n实际坐标 = 板框左边界 + 比例 × 板框宽度；数组元素对应各组。\r\n例：`[0.3333, 0.6667]` 表示两组分别位于板宽 1/3、2/3 处。\r\n不填时按组序均分：第 i 组取 `(i+1)/(组数+1)`。",
      "type": "array",
      "items": {
        "type": "number"
      }
    }
  },
  "required": [
    "on",
    "stampHoleGroupQuantity",
    "stampHoleDiameter",
    "stampHoleQuantityPerGroup",
    "stampHoleSpacing"
  ],
  "description": "邮票孔参数\r\n\r\n拼板时**板与板 / 板与边框之间怎么连**的一种方式：沿板边打一**排小孔**（掰板时沿孔断开）。\r\n一排骨由 `stampHoleGroupQuantity` 组组成、每组若干个孔，各组落在板边的什么位置见\r\n`center_percent`；不启用邮票孔时改用 V-CUT（见 `on` 字段）。\r\n\r\n⚠️ 3.0 里它**不是独立的一行数据**：只作为 `TPanelize` 的 `horizontalStamp` /\r\n`verticalStamp` 两个字段的**内嵌**载体出现（水平、垂直各一份），\r\n**方向由它在 `TPanelize` 里的字段位置表达**。\r\n\r\n其 `id`（如单独出现在盘上）形态见 `TElementId`（普通形态为随机 16 位十六进制）。"
}