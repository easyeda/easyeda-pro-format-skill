{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "name": {
      "description": "模板名称",
      "type": "string"
    }
  },
  "required": [
    "name"
  ],
  "description": "设计规则模板\r\n\r\n**id 为固定单例名 `RULE_TEMPLATE`**（与 `RULE` / `RULE_SELECTOR` 的数组 id 不同）；\r\n模板名在载荷里（`name`）。"
}