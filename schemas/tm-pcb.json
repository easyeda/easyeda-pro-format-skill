{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "board": {
      "description": "所属板子",
      "type": "string"
    },
    "parent": {
      "description": "父 PCB",
      "type": "string"
    },
    "title": {
      "description": "文档名称",
      "type": "string"
    },
    "source": {
      "description": "来源：`<来源条目 uuid>|<来源工程 uuid>` 两段式（工程库独有）\r\n\r\n第 2 段是**来源工程的 uuid**（不是库内路径），**工程内自建条目为空串**。\r\n\r\n读取端拿它去哪查：按 `|` 切开成 `[条目 uuid, 库标识]`，再用第 2 段当 **`path`（库标识）**\r\n去那个库里按第 1 段取条目。",
      "type": "string"
    },
    "zIndex": {
      "description": "排序大小：**可以是 `null`**（`null` 表示「未设置」，是正常取值而非缺字段）\r\n\r\n⚠️ **写侧差异**：原理图 / 仿真原理图 / PCB / 面板写盘时对 0 或缺省一律写 `null`\r\n（`zIndex: data.zIndex || null`）；只有原理图页与仿真页是原样透传。",
      "type": [
        "number",
        "null"
      ]
    }
  },
  "required": [
    "board",
    "parent",
    "title",
    "source",
    "zIndex"
  ],
  "description": "PCB META 类型\r\n\r\nPCB 文档（`PCB`）的 META 行，一行一条（`type:\"META\"`）：文档名、来源、排序，\r\n外加 `board`（**所属板子**）与 `parent`（**父 PCB**，用于分区 / 子 PCB 场景；无父级时写空串）。\r\n\r\nPCB 文档的 META 行 id 是**固定单例名** `META`（属**固定单例 id**，见 `TSingletonElementId`）。"
}