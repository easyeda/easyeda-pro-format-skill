{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "partitionId": {
      "description": "所属分区编号，为null表示无分区",
      "type": [
        "string",
        "null"
      ]
    },
    "groupId": {
      "description": "分组编号：相同值的图元属于同一组；**未分组时的取值见下**。\r\n\r\n ⚠️ **未分组写的是数字 `0`**（不是字符串 `\"0\"`）——真实数据里 `\"groupId\":0` 出现\r\n 5 千余次，而 `\"groupId\":\"0\"` **一次都没有**；FOOTPRINT 文档下该字段**恒为 0**。\r\n 成组时才是字符串（组合 id）；未分组的判定是「`groupId` 为空或 FOOTPRINT 文档」，此时写**数字 0**。\r\n\r\n ⚠️ 上方的类型 `string` 比实际形态窄（装不下数字 0）——**放宽类型会波及 pro-* 仓的\r\n 消费方，需跨仓协调后再改**，故此处暂以说明为准。本文件各 `@example` 里的\r\n `\"groupId\":\"0\"` 也是同一问题，同理按“说明为准”处理。",
      "type": "string"
    },
    "layerId": {
      "description": "层编号",
      "type": "number"
    },
    "locked": {
      "description": "是否锁定",
      "type": "boolean"
    },
    "zIndex": {
      "description": "同层内的叠放次序（渲染排序键，格式里没有高度语义）。\r\n\r\n **它是独立字段，不由 id 推导**：3.0 里未设置时写盘为 **-1**（内部实现即\r\n `_zIndex || -1`；真实数据 `\"zIndex\":-1` 出现 4 千余次）。**例外**：`ATTR` / `META`\r\n 的默认体直接写 `null`。\r\n\r\n 「由 id 尾部数字推导」是 **2.0 的语义**（2.0 的 `getZIndex` + `idReg`；那个正则也\r\n 匹配不了 3.0 的随机 16 位十六进制 id）。**不要照它推导 3.0 的值。**\r\n\r\n ⚠️ **VIA 是个特例：3.0 写盘不会产出该键**——写盘端的 switch 里**没有** zIndex 分支\r\n （虽然槽位表里有该槽位），取值为 `undefined` 时会被 JSON 序列化整个丢弃。\r\n 但**真机存量里仍能遇到 2.0 遗留的带 `zIndex` 的 VIA 行**（样本 15455 条 VIA 中\r\n 10404 条载荷含该键，其值恰好等于 id 尾部的数字——正是上面说的「2.0 语义」）。\r\n **读盘时不要假设它不存在。**",
      "type": [
        "number",
        "null"
      ],
      "default": -1
    },
    "fileName": {
      "description": "文件名",
      "type": "string"
    },
    "startX": {
      "description": "左上 X",
      "type": "number"
    },
    "startY": {
      "description": "左上 Y",
      "type": "number"
    },
    "width": {
      "description": "宽",
      "type": "number"
    },
    "height": {
      "description": "高",
      "type": "number"
    },
    "angle": {
      "description": "旋转角度（角度制），绕 左上 点",
      "type": "number"
    },
    "mirror": {
      "description": "原始图片是否水平镜像，镜像以原始图片 BBox 中点进行水平镜像",
      "type": "boolean"
    },
    "path": {
      "description": "二进制数据 1.一般格式，与 Data Urls 完全兼容 data:[<mediatype>][;base64],<data> 2.BLOB 引用格式 blob:hashid",
      "type": "string"
    },
    "specialColor": {
      "description": "特殊颜色。\r\n\r\n ⚠️ **当前格式里它只是个空壳**：编码 / 解码两侧**都没有它**（属未实现 / 2.0 遗留），写盘不会产出该键、读盘也不会消费。\r\n 字段保留仅为兼容历史数据。**不要据此推断同族字段**——\r\n `TPcbImage` / `TPcbString` / `TPcbDimension` 的 `specialColor` 是**真的会写会读**的。",
      "type": "string",
      "pattern": "^$|^#[0-9A-Fa-f]{6}$"
    }
  },
  "required": [
    "groupId",
    "layerId",
    "locked",
    "zIndex",
    "fileName",
    "startX",
    "startY",
    "width",
    "height",
    "angle",
    "mirror",
    "path"
  ],
  "description": "PCB 二进制内嵌对象"
}