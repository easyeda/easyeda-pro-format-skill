{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "partId": {
      "description": "所属部件编号（**可选**，真机存在整键缺失的 COMPONENT）：\r\n**符号页**上表示自身归属于哪个部件，**图页**的元件则指向它**关联的符号文档**里的部件。\r\n取值即目标 `PART` 行的 `id`（`PART` 行只存在于 SYMBOL 文档，见 `TPart`）。",
      "type": "string"
    },
    "groupId": {
      "description": "分组编号；**两种「未成组」形态都表示未成组**：\r\n- **3.0 写盘时该键根本不存在**：写入端取 `model.combination?.id`，未成组即 `undefined`，\r\n  经 `JSON.stringify` 后整键被丢弃，消费方须**按可选键处理**；\r\n- **`\"\"` 只出现在 2.0 迁移数据里**（2.0 → 3.0 转换腿原样带着它）。",
      "type": "string"
    },
    "locked": {
      "description": "是否锁定。\r\n\r\n⚠️ **原理图侧目前只是数据字段**：pro-sch 里没有任何按它拦截拖拽 / 删除 / 改大小 /\r\n改形状的实现（连「锁定 / 解锁」命令本身都是空实现，只 publish 属性面板刷新）。\r\n那四条行为特征只在 **PCB / 面板**侧落实。生成数据时照常写入本字段，\r\n但**不要指望原理图端会因为它而禁止操作**。",
      "type": "boolean"
    },
    "zIndex": {
      "description": "同层内的叠放次序：渲染排序键，数值越小越靠下（格式里没有高度语义）",
      "type": [
        "number",
        "null"
      ]
    },
    "yAxisDirection": {
      "description": "Y 轴方向标记：**仅 eprj3 本地文件格式会带**，读盘时被剥离。\r\n\r\n取值：`up` = **笛卡尔坐标系（Y 向上）**，本行坐标已按它写出（本地文件固定用它），`down` = **屏幕坐标系（Y 向下）**（编辑器内部与云端的常态）\r\n——**字段缺失等价于它**，两者是同一件事；两套坐标系下同一个形状的 y **互为相反数**。完整语义见 `TYAxisDirection`。\r\n\r\n⚠️ 本基类被多个图元继承，**各自被翻转的字段不同**——完整对照表见 `TYAxisDirection`。\r\n本基类直接覆盖的：`ATTR`/`TEXT`/`PIN`/`COMPONENT` 翻 `y`；`OBJ`/`TABLE` 翻 `startY`；\r\n`ARC` 翻 `startY`/`referY`/`endY`；`CIRCLE`/`ELLIPSE` 翻 `centerY` + 内嵌 `text.y`；\r\n`RECT` 翻 `dotY1`/`dotY2` + `text.y`；`MASK_REGION` 翻 `dotY1`/`dotY2`；`POLY` 翻\r\n`points` 各项的 `y`；`BEZIER` 翻 `controls` 的奇数下标。\r\n`rotation` / `isMirror` / `zIndex` 与所有幅值字段**不翻**。",
      "type": "string",
      "enum": [
        "up",
        "down"
      ]
    },
    "x": {
      "description": "文本坐标 X（0.01 inch）",
      "type": "number"
    },
    "y": {
      "description": "文本坐标 Y（0.01 inch）",
      "type": "number"
    },
    "rotation": {
      "description": "旋转角度（角度制，**逆时针为正**），绕 文本坐标 旋转\r\n\r\n注意：本字段在 RECT / ELLIPSE 上是**图形旋转角的实际载体**——这两个类型的\r\n顶层 `rotation` 已废弃（写盘恒 0），旋转值写在这里；CIRCLE 没有顶层\r\nrotation 字段，旋转角同样由这里承载。",
      "type": "number",
      "minimum": 0,
      "maximum": 360
    },
    "value": {
      "description": "文本内容：任意字符",
      "type": "string"
    },
    "version": {
      "description": "表示来自 2.0 版本",
      "type": "string",
      "enum": [
        "2.0"
      ]
    },
    "color": {
      "description": "字体颜色：`\"#RRGGBB\"` 十六进制色值；null 表示采用主题默认色",
      "type": [
        "string",
        "null"
      ]
    },
    "fillColor": {
      "description": "字体背景色：`\"#RRGGBB\"` 十六进制色值；null 表示无背景 / 采用主题默认\r\n\r\n⚠️ **语义随宿主而变**：只有 **TABLE 单元格**上的它才是真正的背景色；\r\n在 TEXT / ATTR 上它与 `color` 同源（同一个颜色字段），且**解码端根本不读它**\r\n（只读 `color`），不要把它当独立字段依赖。",
      "type": [
        "string",
        "null"
      ]
    },
    "fontFamily": {
      "description": "字体名称",
      "type": [
        "string",
        "null"
      ]
    },
    "fontSize": {
      "description": "字体大小，与坐标等单位相同",
      "type": [
        "number",
        "null"
      ]
    },
    "strikeout": {
      "description": "是否加删除线",
      "type": [
        "boolean",
        "null"
      ]
    },
    "underline": {
      "description": "是否加下划线",
      "type": [
        "boolean",
        "null"
      ]
    },
    "italic": {
      "description": "是否斜体",
      "type": [
        "boolean",
        "null"
      ]
    },
    "fontWeight": {
      "description": "是否加粗",
      "type": [
        "boolean",
        "null"
      ]
    },
    "align": {
      "description": "对齐模式",
      "type": "string",
      "enum": [
        "LEFT_BOTTOM",
        "CENTER_BOTTOM",
        "RIGHT_BOTTOM",
        "LEFT_MIDDLE",
        "CENTER_MIDDLE",
        "RIGHT_MIDDLE",
        "LEFT_TOP",
        "CENTER_TOP",
        "RIGHT_TOP"
      ],
      "enumDescriptions": {
        "LEFT_BOTTOM": "左底",
        "CENTER_BOTTOM": "中底",
        "RIGHT_BOTTOM": "右底",
        "LEFT_MIDDLE": "左中",
        "CENTER_MIDDLE": "中中",
        "RIGHT_MIDDLE": "右中",
        "LEFT_TOP": "左顶",
        "CENTER_TOP": "中顶",
        "RIGHT_TOP": "右顶"
      }
    }
  },
  "required": [
    "groupId",
    "locked",
    "zIndex",
    "x",
    "y",
    "rotation",
    "value",
    "color",
    "fillColor",
    "fontFamily",
    "fontSize",
    "strikeout",
    "underline",
    "italic",
    "fontWeight",
    "align"
  ],
  "description": "文本\r\n\r\n**原理图族里的一段画布文字**，一行一条（`type:\"TEXT\"`）：图上写的说明 / 注释，\r\n只按自己的坐标与字体样式绘制，**不挂宿主**（这一点与器件属性 `TSchAttr` 不同）。\r\n\r\n样式来自 `TFontStyle`；文本锚在 `x` / `y` 上，`rotation` 是**绕该点逆时针**的角度。\r\n`version` 为 `'2.0'` 表示这条数据由 2.0 转换而来——两版的**文本原点解析不一致**，靠它区分。\r\n\r\n⚠️ 本类型也是 CIRCLE / RECT / ELLIPSE 内嵌 `text` 字段的来源，但**盘上内嵌的那份是缩减形态**：\r\n`align` 与 `groupId` / `locked` / `zIndex` 恒为 `null`，另多一个本类型**没有收录**的\r\n`lineHeight`（行高倍数，`null` = 主题默认）——那是有意保留的扩展字段，解码端会读。\r\n\r\n本行的 `id` 由编辑器生成（随机 16 位小写十六进制），图元 id 的几类形态与判别见 `TElementId`。"
}