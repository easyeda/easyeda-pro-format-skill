{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "lineGroup": {
      "description": "所属的导线或总线组 id",
      "type": "string",
      "pattern": "^[0-9a-f]{16}$"
    },
    "startX": {
      "description": "起点坐标 X（0.01 inch）",
      "type": "number"
    },
    "startY": {
      "description": "起点坐标 Y（0.01 inch）",
      "type": "number"
    },
    "endX": {
      "description": "结束坐标 X（0.01 inch）",
      "type": "number"
    },
    "endY": {
      "description": "结束坐标 Y（0.01 inch）",
      "type": "number"
    },
    "yAxisDirection": {
      "description": "Y 轴方向标记：**仅 eprj3 本地文件格式会带**，读盘时被剥离。\r\n\r\n取值：`up` = **笛卡尔坐标系（Y 向上）**，本行坐标已按它写出（本地文件固定用它），`down` = **屏幕坐标系（Y 向下）**（编辑器内部与云端的常态）\r\n——**字段缺失等价于它**，两者是同一件事；两套坐标系下同一个形状的 y **互为相反数**。完整语义见 `TYAxisDirection`。\r\n\r\n本类型（`LINE`）翻转的字段是 **`startY` 与 `endY`**（`startX`/`endX` 不动）。",
      "type": "string",
      "enum": [
        "up",
        "down"
      ]
    },
    "strokeColor": {
      "description": "描边颜色：`\"#RRGGBB\"` 十六进制色值；**null 表示采用主题默认色**\r\n\r\n注：本文件各示例中的该字段**一律为 null**，非空色的具体串格式未在示例中出现；\r\n可参照同文件 `TSchPin.color` 的 `@pattern ^$|^#[0-9A-Fa-f]{6}$`。",
      "type": [
        "string",
        "null"
      ]
    },
    "strokeStyle": {
      "description": "样式, null 为默认",
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "SOLID",
        "SHORT_DASH",
        "DOT",
        "DOT_DASH",
        null
      ],
      "enumDescriptions": {
        "SOLID": "实线",
        "SHORT_DASH": "短划线",
        "DOT": "点线",
        "DOT_DASH": "点划线"
      },
      "default": null
    },
    "fillColor": {
      "description": "填充颜色：`\"#RRGGBB\"` 十六进制色值；`\"\"` 表示不填充（填充会自动闭合起始点与结束点）；\r\nnull 表示采用主题默认",
      "type": [
        "string",
        "null"
      ]
    },
    "strokeWidth": {
      "description": "宽度：null 表示采用主题默认线宽\r\n单位：**0.01 inch**",
      "type": [
        "number",
        "null"
      ],
      "default": null
    },
    "fillStyle": {
      "description": "填充样式, null 为默认",
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "NONE",
        "SOLID",
        "GRID",
        "HORIZONTAL_LINE",
        "VERTICAL_LINE",
        "RHOMBIC",
        "LEFT_SLASH_LINE",
        "RIGHT_SLASH_LINE",
        null
      ],
      "enumDescriptions": {
        "NONE": "无填充",
        "SOLID": "实心填充",
        "GRID": "网格",
        "HORIZONTAL_LINE": "横线",
        "VERTICAL_LINE": "竖线",
        "RHOMBIC": "菱形网格",
        "LEFT_SLASH_LINE": "左斜线",
        "RIGHT_SLASH_LINE": "右斜线"
      },
      "default": null
    }
  },
  "required": [
    "lineGroup",
    "startX",
    "startY",
    "endX",
    "endY",
    "strokeColor",
    "strokeStyle",
    "fillColor",
    "strokeWidth",
    "fillStyle"
  ],
  "description": "单线段\r\n\r\n**原理图族里的一根单线段**，一行一条（`type:\"LINE\"`）——⚠️ **它不是独立图元**：\r\n导线 / 总线都是「组 + 若干线段」的结构，线段靠 `lineGroup` 挂在某个 `TWire` / `TBus` 上，\r\n脱离组单独存在就是悬空引用。\r\n\r\n线段只有起止两点、不能拐弯：一条折线形的导线由多条 `LINE` 按同组顺序拼接而成，\r\n样式（描边 / 填充）来自 `TLineStyle`。\r\n\r\n【联动增删·必须成组操作】与所属组**同生命周期**：新增 WIRE / BUS 时要一并写出它的 LINE，\r\n删除时要一并删除；同组的 `TSchAttr`（承载 NET 名等）也属同一生命周期，要一起增删。\r\n\r\n本行的 `id` 由编辑器生成（随机 16 位小写十六进制，文档内唯一）；`lineGroup` 指向所属\r\nWIRE / BUS 的 `id`。图元 id 的几类形态与判别见 `TElementId`。"
}