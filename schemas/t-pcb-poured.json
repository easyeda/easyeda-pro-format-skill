{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "pourFill": {
      "description": "",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "description": "唯一标识",
            "type": "string"
          },
          "strokeWidth": {
            "description": "描边线宽：0 为不描边（mil）",
            "type": "number"
          },
          "fill": {
            "description": "是否填充",
            "type": "boolean"
          },
          "path": {
            "description": "填充结果轮廓：数组的每个元素是**一条独立的单多边形**（`TPcbSinglePolygon`），各元素并列存在、\r\n**不是**「外轮廓 + 内洞」的嵌套结构（此处数组只有一层）。\r\n\r\n⚠️ **此处不会出现 `[\"R\", …]` 矩形形态**（与 `TPcbPour.path` 等不同）。\r\n本字段由 `getPolygonsPath` 产出，该函数只产 `CIRCLE` 或指令序列；\r\n多边形解析只产出 `['CIRCLE', …]`，全文无任何 `'R'` 产出点。**不要照 `TPcbPour`\r\n的文档给这里写 R 形态。**",
            "type": "array"
          }
        },
        "required": [
          "id",
          "strokeWidth",
          "fill",
          "path"
        ],
        "description": "覆铜填充结果"
      }
    }
  },
  "required": [
    "pourFill"
  ],
  "description": "覆铜结果\r\n\r\n**为什么要把覆铜结果单独落盘**：对一块较大的 PCB，每次打开都重算覆铜是不现实的，\r\n所以把计算结果缓存成独立文档。\r\n\r\n结果里每一段路径分**填充**与**描边**两类用途：\r\n- **填充**：一般对应实心填充的填充部分；\r\n- **描边**：一般对应热焊盘、网格覆铜，以及实心填充「制造优化」模式下的描边等。\r\n\r\n【外层数据id构造】id 是**键式 id** —— 盘上是数组串 `[\"POURED\", 覆铜边框 id]`，\r\n两段各有含义（见 `TKeyedElementId`）：\r\n**第 1 段** `\"POURED\"` 是固定前缀；**第 2 段**是**它所属那条覆铜边框（`POUR` 行）的图元 id**\r\n（`TPcbPour` 的外壳 `id`，普通形态的随机 hex）。所以每条覆铜结果都**挂在它的父 POUR 名下**。\r\n\r\n【联动增删·必须成组操作】POURED 是 POUR（`TPcbPour`）的**计算结果**，靠上面那个 id 挂在其父 POUR 下：\r\n- **有 POUR 才写 POURED**；**删除 POUR 时必须一并删除它的 POURED**，否则残留悬空记录。"
}