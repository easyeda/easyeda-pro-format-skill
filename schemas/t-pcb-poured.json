{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "pourFill": {
      "description": "",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "description": "唯一标识",
            "type": "string"
          },
          "strokeWidth": {
            "description": "描边线宽：0 为不描边",
            "type": "number"
          },
          "fill": {
            "description": "是否填充",
            "type": "boolean"
          },
          "path": {
            "description": "填充结果轮廓：数组的每个元素是**一条独立的单多边形**，各元素并列存在、\r\n **不是**「外轮廓 + 内洞」的嵌套结构（此处数组只有一层）。\r\n\r\n 【单多边形结构】每个元素是扁平数组：前两个数字是起点坐标，其后是\r\n 「指令名 + 该指令参数」的重复序列——\r\n - `L` 直线：`x y L x y x y ...`，各点依次直线相连\r\n - `ARC` 圆弧：`startX startY ARC angle endX endY`（angle 逆时针为正、顺时针为负）\r\n - `CARC` 中心圆弧：`startX startY CARC angle endX endY`\r\n - `C` 三阶贝塞尔：`x1 y1 C x2 y2 x3 y3 x4 y4 ...`，四个一组为一条曲线\r\n\r\n 另有一种与上述模式互斥的独立形态：\r\n - `[\"CIRCLE\", cx, cy, r]` 整圆\r\n\r\n ⚠️ **此处不会出现 `[\"R\", …]` 矩形形态**（与 `TPcbPour.path` 等不同）。\r\n 本字段由 `getPolygonsPath` 产出，该函数只产 `CIRCLE` 或指令序列；\r\n 多边形解析只产出 `['CIRCLE', …]`，全文无任何 `'R'` 产出点。**不要照 `TPcbPour`\r\n 的文档给这里写 R 形态。**",
            "type": "array",
            "items": {
              "type": "array"
            }
          }
        },
        "required": [
          "id",
          "strokeWidth",
          "fill",
          "path"
        ],
        "description": "覆铜填充结果"
      }
    }
  },
  "required": [
    "pourFill"
  ],
  "description": "覆铜结果\r\n\r\n【联动增删·必须成组操作】POURED 是 POUR 的**计算结果**，靠 id 挂在其父 POUR 下\r\n（id 形如 `[\"POURED\", <POUR 的 id>]`）：\r\n- **有 POUR 才写 POURED**；**删除 POUR 时必须一并删除它的 POURED**，否则残留悬空记录。"
}