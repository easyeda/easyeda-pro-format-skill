{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "partitionId": {
      "description": "所属分区编号，为null表示无分区：**值是 `PARTITION` 行的外壳 `id`**（图元 id，不是 uuid）",
      "type": [
        "string",
        "null"
      ]
    },
    "groupId": {
      "description": "分组编号：相同值的图元属于同一组；**未分组时的取值见下**。\r\n\r\n⚠️ **未分组写的是数字 `0`**（不是字符串 `\"0\"`）——真实数据里 `\"groupId\":0` 出现\r\n5 千余次，而 `\"groupId\":\"0\"` **一次都没有**；FOOTPRINT 文档下该字段**恒为 0**。\r\n成组时才是字符串（组合 id）；未分组的判定是「`groupId` 为空或 FOOTPRINT 文档」，此时写**数字 0**。\r\n\r\n⚠️ 上方的类型 `string` 比实际形态窄（装不下数字 0）——**放宽类型会波及 pro-* 仓的\r\n消费方，需跨仓协调后再改**，故此处暂以说明为准。本文件各 `@example` 里的\r\n`\"groupId\":\"0\"` 也是同一问题，同理按“说明为准”处理。",
      "type": "string"
    },
    "layerId": {
      "description": "层编号。\r\n\r\n**第几号是哪一层见 `ELayerCode`** —— 那是层编号的枚举（`1` = 顶层、`47` = 孔层 …），\r\n里面逐条写了每个编号对应的**层类型字符串名**。\r\n\r\n⚠️ 类型写成 **`number | ELayerCode`** 而不是单用枚举：实际数据里层号本来就是普通数字，\r\n消费方大量按 `number` 传值，收紧成枚举会让那些构造点编译不过。",
      "type": "number"
    },
    "locked": {
      "description": "是否锁定：锁定后**不能拖拽、不能删除、不能用鼠标键盘调整大小、不能调整形状**。\r\n实际是以**锁弹窗**（`ActionForLockedObjectsDialog`）拦截，用户可在弹窗里选\r\n「解锁并继续」，所以不是硬禁止。",
      "type": "boolean"
    },
    "zIndex": {
      "description": "同层内的叠放次序（渲染排序键，格式里没有高度语义）。\r\n\r\n**它是独立字段，不由 id 推导**：3.0 里未设置时写盘为 **-1**（内部实现即\r\n`_zIndex || -1`；真实数据 `\"zIndex\":-1` 出现 4 千余次）。**例外**：`ATTR` / `META`\r\n的默认体直接写 `null`。\r\n\r\n「由 id 尾部数字推导」是 **2.0 的语义**（2.0 的 `getZIndex` + `idReg`；那个正则也\r\n匹配不了 3.0 的随机 16 位十六进制 id）。**不要照它推导 3.0 的值。**\r\n\r\n⚠️ **VIA 是个特例：3.0 写盘不会产出该键**——写盘端的 switch 里**没有** zIndex 分支\r\n（虽然槽位表里有该槽位），取值为 `undefined` 时会被 JSON 序列化整个丢弃。\r\n但**真机存量里仍能遇到 2.0 遗留的带 `zIndex` 的 VIA 行**（样本 15455 条 VIA 中\r\n10404 条载荷含该键，其值恰好等于 id 尾部的数字——正是上面说的「2.0 语义」）。\r\n**读盘时不要假设它不存在。**",
      "type": [
        "number",
        "null"
      ],
      "default": -1
    },
    "type": {
      "description": "尺寸类型。",
      "type": "string",
      "enum": [
        "LENGTH",
        "RADIUS",
        "ANGLE"
      ],
      "enumDescriptions": {
        "LENGTH": "长度",
        "RADIUS": "半径",
        "ANGLE": "角度"
      }
    },
    "unit": {
      "description": "单位。",
      "type": "string",
      "enum": [
        "mil",
        "inch",
        "cm",
        "mm"
      ],
      "enumDescriptions": {
        "mil": "密耳",
        "inch": "英寸",
        "cm": "厘米",
        "mm": "毫米"
      }
    },
    "strokeWidth": {
      "description": "线宽（mil）",
      "type": "number"
    },
    "precision": {
      "description": "精度",
      "type": "number",
      "minimum": 0
    },
    "textFollow": {
      "description": "文字是否跟随。\r\n\r\n⚠️ **它实际是个死字段**：写出端**恒写数字 `1`**，解码端解构后**根本不读它**。\r\n**真正决定「文字跟随」的是 `text.x === null`**——解码时由 x/y 是否为 null\r\n推导 `positionIsNull`（`AttrDecode`）。所以要表达语义请写 `text.x`，\r\n本字段照写 `1` 即可。",
      "type": "boolean"
    },
    "coords": {
      "description": "坐标集：扁平数组，**元素的个数与含义随 `type` 而变**——\r\n- `LENGTH`（长度）：8 个数，依次为 左点 `x y`、起点 `x y`、终点 `x y`、右点 `x y`\r\n- `RADIUS`（半径）：5 个数，依次为 终点 `x y`、起点 `x y`、半径\r\n- `ANGLE`（角度）：6 个数，依次为 起点 `x y`、圆心 `x y`、终点 `x y`",
      "type": "array",
      "items": {
        "type": "number"
      }
    },
    "text": {
      "description": "文本。\r\n\r\n⚠️ **类型/示例声明为完整的 TPcbString，实际写盘却只有 15 个键**——\r\n`layerId` / `x` / `y` / `text` / `fontFamily` / `fontSize` / `strokeWidth` /\r\n`bold` / `italic` / `origin` / `angle` / `reverse` / `expansion` / `mirror` / `locked`\r\n（编码端 key 表）。\r\n**盘上不会出现** `partitionId` / `groupId` / `zIndex` / `specialColor`，\r\n往这四项塞东西**不会落盘**。\r\n另：处于「跟随」态时 `x` / `y` 会被写成 `null`。",
      "type": "object",
      "properties": {
        "partitionId": {
          "description": "所属分区编号，为null表示无分区：**值是 `PARTITION` 行的外壳 `id`**（图元 id，不是 uuid）",
          "type": [
            "string",
            "null"
          ]
        },
        "groupId": {
          "description": "分组编号：相同值的图元属于同一组；**未分组时的取值见下**。\r\n\r\n⚠️ **未分组写的是数字 `0`**（不是字符串 `\"0\"`）——真实数据里 `\"groupId\":0` 出现\r\n5 千余次，而 `\"groupId\":\"0\"` **一次都没有**；FOOTPRINT 文档下该字段**恒为 0**。\r\n成组时才是字符串（组合 id）；未分组的判定是「`groupId` 为空或 FOOTPRINT 文档」，此时写**数字 0**。\r\n\r\n⚠️ 上方的类型 `string` 比实际形态窄（装不下数字 0）——**放宽类型会波及 pro-* 仓的\r\n消费方，需跨仓协调后再改**，故此处暂以说明为准。本文件各 `@example` 里的\r\n`\"groupId\":\"0\"` 也是同一问题，同理按“说明为准”处理。",
          "type": "string"
        },
        "layerId": {
          "description": "层编号。\r\n\r\n**第几号是哪一层见 `ELayerCode`** —— 那是层编号的枚举（`1` = 顶层、`47` = 孔层 …），\r\n里面逐条写了每个编号对应的**层类型字符串名**。\r\n\r\n⚠️ 类型写成 **`number | ELayerCode`** 而不是单用枚举：实际数据里层号本来就是普通数字，\r\n消费方大量按 `number` 传值，收紧成枚举会让那些构造点编译不过。",
          "type": "number"
        },
        "locked": {
          "description": "是否锁定：锁定后**不能拖拽、不能删除、不能用鼠标键盘调整大小、不能调整形状**。\r\n实际是以**锁弹窗**（`ActionForLockedObjectsDialog`）拦截，用户可在弹窗里选\r\n「解锁并继续」，所以不是硬禁止。",
          "type": "boolean"
        },
        "zIndex": {
          "description": "同层内的叠放次序（渲染排序键，格式里没有高度语义）。\r\n\r\n**它是独立字段，不由 id 推导**：3.0 里未设置时写盘为 **-1**（内部实现即\r\n`_zIndex || -1`；真实数据 `\"zIndex\":-1` 出现 4 千余次）。**例外**：`ATTR` / `META`\r\n的默认体直接写 `null`。\r\n\r\n「由 id 尾部数字推导」是 **2.0 的语义**（2.0 的 `getZIndex` + `idReg`；那个正则也\r\n匹配不了 3.0 的随机 16 位十六进制 id）。**不要照它推导 3.0 的值。**\r\n\r\n⚠️ **VIA 是个特例：3.0 写盘不会产出该键**——写盘端的 switch 里**没有** zIndex 分支\r\n（虽然槽位表里有该槽位），取值为 `undefined` 时会被 JSON 序列化整个丢弃。\r\n但**真机存量里仍能遇到 2.0 遗留的带 `zIndex` 的 VIA 行**（样本 15455 条 VIA 中\r\n10404 条载荷含该键，其值恰好等于 id 尾部的数字——正是上面说的「2.0 语义」）。\r\n**读盘时不要假设它不存在。**",
          "type": [
            "number",
            "null"
          ],
          "default": -1
        },
        "x": {
          "description": "位置 X（mil）",
          "type": "number"
        },
        "y": {
          "description": "位置 Y（mil）",
          "type": "number"
        },
        "text": {
          "description": "内容",
          "type": "string"
        },
        "fontFamily": {
          "description": "字体名称",
          "type": "string"
        },
        "fontSize": {
          "description": "字号\r\n 单位：**mil**",
          "type": "number",
          "minimum": 1
        },
        "strokeWidth": {
          "description": "粗细\r\n 单位：**mil**",
          "type": "number",
          "minimum": 0
        },
        "bold": {
          "description": "是否加粗。\r\n\r\n⚠️ **写盘恒为数字 `0`**（编码器直接 `return 0`，不是布尔），解码端解构后\r\n**从不赋值**该字段 —— 也就是说当前版本它**恒为 0、读出来等于未启用**。",
          "type": "boolean"
        },
        "italic": {
          "description": "是否斜体。口径同 `bold`：**写盘恒为数字 `0`、解码忽略**。",
          "type": "boolean"
        },
        "origin": {
          "description": "对齐模式。",
          "type": "string",
          "enum": [
            "LEFT_BOTTOM",
            "CENTER_BOTTOM",
            "RIGHT_BOTTOM",
            "LEFT_MIDDLE",
            "CENTER_MIDDLE",
            "RIGHT_MIDDLE",
            "LEFT_TOP",
            "CENTER_TOP",
            "RIGHT_TOP"
          ],
          "enumDescriptions": {
            "LEFT_BOTTOM": "左底",
            "CENTER_BOTTOM": "中底",
            "RIGHT_BOTTOM": "右底",
            "LEFT_MIDDLE": "左中",
            "CENTER_MIDDLE": "中中",
            "RIGHT_MIDDLE": "右中",
            "LEFT_TOP": "左顶",
            "CENTER_TOP": "中顶",
            "RIGHT_TOP": "右顶"
          }
        },
        "angle": {
          "description": "旋转角度（角度制，**逆时针为正**）",
          "type": "number",
          "minimum": 0
        },
        "reverse": {
          "description": "是否反相扩展",
          "type": "boolean"
        },
        "expansion": {
          "description": "反相扩展尺寸：反相扩展区域的尺寸，支持负数",
          "type": "number"
        },
        "mirror": {
          "description": "是否镜像，一般来说 ，当一个文字出现在底层，这里也需要相应调整成 1",
          "type": "boolean"
        },
        "specialColor": {
          "description": "特殊颜色。**不是可以随便填的自由字段**：只在**顶层 / 底层丝印层**\r\n（`layerId` 为 TopSilk / BottomSilk）**且模型带有丝印色**（`silkColor != null`）时\r\n才会写出，否则**恒为 `null`**。**生成数据时不要指望它一定落盘。**",
          "type": "string",
          "pattern": "^$|^#[0-9A-Fa-f]{6}$"
        }
      },
      "required": [
        "groupId",
        "layerId",
        "locked",
        "zIndex",
        "x",
        "y",
        "text",
        "fontFamily",
        "fontSize",
        "strokeWidth",
        "bold",
        "italic",
        "origin",
        "angle",
        "reverse",
        "expansion",
        "mirror"
      ]
    },
    "specialColor": {
      "description": "特殊颜色。**不是可以随便填的自由字段**：只在**顶层 / 底层丝印层**\r\n（`layerId` 为 TopSilk / BottomSilk）、**模型带有丝印色**（`silkColor != null`）、\r\n**且 `getParent() == null`** 时才写出，否则**恒为 `null`**。",
      "type": "string",
      "pattern": "^$|^#[0-9A-Fa-f]{6}$"
    }
  },
  "required": [
    "groupId",
    "layerId",
    "locked",
    "zIndex",
    "type",
    "unit",
    "strokeWidth",
    "precision",
    "textFollow",
    "coords",
    "text"
  ],
  "description": "PCB 尺寸工具集\r\n\r\nPCB / 封装域里的**尺寸标注**，一行一条（`type:\"DIMENSION\"`）：量出两点的**长度**、\r\n圆弧的**半径**或**角度**，并把读数画在画布上。\r\n\r\n标什么由 `type` 决定，几何由 `coords` 给出 —— ⚠️ **`coords` 是扁平数组、元素个数与含义\r\n随 `type` 而变**（长度 8 个 / 半径 5 个 / 角度 6 个，顺序见字段说明）。\r\n\r\n读数文字是**内嵌**的 `text`（一个完整文字图元，文本已按 `unit` / `precision` 渲染好），\r\n不是独立的一行；「文字跟随」也不看 `textFollow`（死字段），而是看 `text.x` 是否为 null。\r\n\r\nid 由编辑器生成（随机 16 位十六进制），形态见 `TElementId`。"
}