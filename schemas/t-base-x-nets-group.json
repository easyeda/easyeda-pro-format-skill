{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "netLengthPhysic": {
      "description": "网络长度规则 --- 对应组的",
      "type": "string"
    },
    "netLengthTolerancePhysics": {
      "description": "网络长度公差规则 --- 对应组的",
      "type": "string"
    },
    "targetNet": {
      "description": "目标网络",
      "type": "string"
    }
  },
  "required": [
    "netLengthPhysic",
    "netLengthTolerancePhysics",
    "targetNet"
  ],
  "description": "xNet 组\r\n\r\n【联动增删·必须成组操作】X_NET_GROUP 与其组内的 X_NET 是**同一生命周期**：\r\n**新增组时必须一并写出组内的 X_NET；删除 X_NET_GROUP 时必须把组内所有 X_NET 一并删除**\r\n（组内 xNet 清空后才能删组），否则留下悬空的 X_NET。\r\n\r\n【外层数据id构造】id 是**数组串** `[\"X_NET_GROUP\", 组名]`，两段各有含义（见 `TKeyedElementId`）：\r\n**第 1 段** `\"X_NET_GROUP\"` 是固定前缀；**第 2 段**是**这个 xNet 组的名字**，\r\n即用户在工程里给这一组网络起的名字（属性面板上可改的那个文本，**不是任何行的 id**）。\r\n它来自本类型的字段 `groupsName`，但**写盘时 `groupsName` 会从载荷里删掉**（只存在于 id），\r\n读盘时再从 id 还原；所以本页的载荷字段表里**看不到它，不要以为是漏写**（同族的归一化见 `TPcbXNets`）。\r\n数组串是**键式 id**（见 `TKeyedElementId`）。"
}