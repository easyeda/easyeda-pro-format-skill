{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "partId": {
      "description": "所属部件编号（**可选**，真机存在整键缺失的 COMPONENT）：\r\n **符号页**上表示自身归属于哪个部件，**图页**的元件则指向它**关联的符号文档**里的部件。\r\n 取值即目标 `PART` 行的 `id`（`PART` 行只存在于 SYMBOL 文档，见 `TPart`）。",
      "type": "string"
    },
    "groupId": {
      "description": "分组编号；**两种「未成组」形态都表示未成组**：\r\n - **3.0 写盘时该键根本不存在**：写入端取 `model.combination?.id`，未成组即 `undefined`，\r\n   经 `JSON.stringify` 后整键被丢弃，消费方须**按可选键处理**；\r\n - **`\"\"` 只出现在 2.0 迁移数据里**（2.0 → 3.0 转换腿原样带着它）。",
      "type": "string"
    },
    "locked": {
      "description": "是否锁定",
      "type": "boolean"
    },
    "zIndex": {
      "description": "同层内的叠放次序：渲染排序键，数值越小越靠下（格式里没有高度语义）",
      "type": [
        "number",
        "null"
      ]
    },
    "yAxisDirection": {
      "description": "Y 轴方向标记：**仅 eprj3 本地文件格式会带**，读盘时被剥离。\r\n\r\n 取值：`up` **已按 Y 轴向上写出**（本地文件固定用它）、`down` Y 轴向下\r\n （**字段缺失等价于它**，两者是同一件事）。完整语义见 `TYAxisDirection`。\r\n\r\n ⚠️ 本基类被多个图元继承，**各自被翻转的字段不同**——完整对照表见 `TYAxisDirection`。\r\n 本基类直接覆盖的：`ATTR`/`TEXT`/`PIN`/`COMPONENT` 翻 `y`；`OBJ`/`TABLE` 翻 `startY`；\r\n `ARC` 翻 `startY`/`referY`/`endY`；`CIRCLE`/`ELLIPSE` 翻 `centerY` + 内嵌 `text.y`；\r\n `RECT` 翻 `dotY1`/`dotY2` + `text.y`；`MASK_REGION` 翻 `dotY1`/`dotY2`；`POLY` 翻\r\n `points` 各项的 `y`；`BEZIER` 翻 `controls` 的奇数下标。\r\n `rotation` / `isMirror` / `zIndex` 与所有幅值字段**不翻**。",
      "type": "string",
      "enum": [
        "up",
        "down"
      ]
    },
    "centerX": {
      "description": "中点 centerX",
      "type": "number"
    },
    "centerY": {
      "description": "中点 centerY",
      "type": "number"
    },
    "radiusX": {
      "description": "水平半径 radiusX\r\n\r\n ⚠️ **真机存在负值**（样本 `\"radiusX\":2.5` 配 `\"radiusY\":-2.5`，来自 2.0 迁移）。\r\n 解码端一律取绝对值，负号无意义；**生成时写正数即可**。",
      "type": "number",
      "minimum": 0
    },
    "radiusY": {
      "description": "垂直半径 radiusY\r\n\r\n ⚠️ **真机存在负值**（样本 `\"radiusX\":2.5` 配 `\"radiusY\":-2.5`，来自 2.0 迁移）。\r\n 解码端一律取绝对值，负号无意义；**生成时写正数即可**。",
      "type": "number",
      "minimum": 0
    },
    "rotation": {
      "description": "旋转角度（角度制）",
      "type": "number",
      "minimum": 0,
      "maximum": 360
    },
    "text": {
      "description": "3.3 添加文本字段。\r\n\r\n 注意：本图元的**旋转角写在 `text.rotation`**（顶层 rotation 已废弃或不存在），\r\n 详见 TSchText.rotation 的说明\r\n\r\n ⚠️ **这是写入端产出的「缩减文本」，不是完整的 `TSchText`**：\r\n - 实际只填 `value` / `color` / `fontSize` / `rotation` / `x` / `y` / `fontFamily` 等；\r\n - **`align` 与 `groupId` / `locked` / `zIndex` 恒为 `null`**（写入端就直接落 `null`），\r\n   解码端**也不读 `align`**——不要照 `TSchText` 的必填口径去要求它们；\r\n - 另带一个 **`lineHeight`**（行高倍数，`null` = 主题默认）：它是**有意设计的扩展字段**\r\n   （协议 `TSchText` 暂未收录，源码序列化时以扩展字段形式读写），解码端会读；\r\n - ⚠️ 内嵌 `text.rotation` **必须写数字，不能写 `null`**：解析判据是\r\n   `Number.isFinite(+data.text.rotation)`，而 `+null === 0` 也算有限值，\r\n   写 `null` 会把图形旋转**静默置 0**。",
      "type": "object",
      "properties": {
        "partId": {
          "description": "所属部件编号（**可选**，真机存在整键缺失的 COMPONENT）：\r\n **符号页**上表示自身归属于哪个部件，**图页**的元件则指向它**关联的符号文档**里的部件。\r\n 取值即目标 `PART` 行的 `id`（`PART` 行只存在于 SYMBOL 文档，见 `TPart`）。",
          "type": "string"
        },
        "groupId": {
          "description": "分组编号；**两种「未成组」形态都表示未成组**：\r\n - **3.0 写盘时该键根本不存在**：写入端取 `model.combination?.id`，未成组即 `undefined`，\r\n   经 `JSON.stringify` 后整键被丢弃，消费方须**按可选键处理**；\r\n - **`\"\"` 只出现在 2.0 迁移数据里**（2.0 → 3.0 转换腿原样带着它）。",
          "type": "string"
        },
        "locked": {
          "description": "是否锁定",
          "type": "boolean"
        },
        "zIndex": {
          "description": "同层内的叠放次序：渲染排序键，数值越小越靠下（格式里没有高度语义）",
          "type": [
            "number",
            "null"
          ]
        },
        "yAxisDirection": {
          "description": "Y 轴方向标记：**仅 eprj3 本地文件格式会带**，读盘时被剥离。\r\n\r\n 取值：`up` **已按 Y 轴向上写出**（本地文件固定用它）、`down` Y 轴向下\r\n （**字段缺失等价于它**，两者是同一件事）。完整语义见 `TYAxisDirection`。\r\n\r\n ⚠️ 本基类被多个图元继承，**各自被翻转的字段不同**——完整对照表见 `TYAxisDirection`。\r\n 本基类直接覆盖的：`ATTR`/`TEXT`/`PIN`/`COMPONENT` 翻 `y`；`OBJ`/`TABLE` 翻 `startY`；\r\n `ARC` 翻 `startY`/`referY`/`endY`；`CIRCLE`/`ELLIPSE` 翻 `centerY` + 内嵌 `text.y`；\r\n `RECT` 翻 `dotY1`/`dotY2` + `text.y`；`MASK_REGION` 翻 `dotY1`/`dotY2`；`POLY` 翻\r\n `points` 各项的 `y`；`BEZIER` 翻 `controls` 的奇数下标。\r\n `rotation` / `isMirror` / `zIndex` 与所有幅值字段**不翻**。",
          "type": "string",
          "enum": [
            "up",
            "down"
          ]
        },
        "x": {
          "description": "文本坐标 X",
          "type": "number"
        },
        "y": {
          "description": "文本坐标 Y",
          "type": "number"
        },
        "rotation": {
          "description": "旋转角度（角度制），绕 文本坐标 旋转\r\n\r\n注意：本字段在 RECT / ELLIPSE 上是**图形旋转角的实际载体**——这两个类型的\r\n顶层 `rotation` 已废弃（写盘恒 0），旋转值写在这里；CIRCLE 没有顶层\r\nrotation 字段，旋转角同样由这里承载。",
          "type": "number",
          "minimum": 0,
          "maximum": 360
        },
        "value": {
          "description": "文本内容：任意字符",
          "type": "string"
        },
        "version": {
          "description": "表示来自 2.0 版本",
          "type": "string",
          "enum": [
            "2.0"
          ]
        },
        "color": {
          "description": "字体颜色：`\"#RRGGBB\"` 十六进制色值；null 表示采用主题默认色",
          "type": [
            "string",
            "null"
          ]
        },
        "fillColor": {
          "description": "字体背景色：`\"#RRGGBB\"` 十六进制色值；null 表示无背景 / 采用主题默认\r\n\r\n ⚠️ **语义随宿主而变**：只有 **TABLE 单元格**上的它才是真正的背景色；\r\n 在 TEXT / ATTR 上它与 `color` 同源（同一个颜色字段），且**解码端根本不读它**\r\n （只读 `color`），不要把它当独立字段依赖。",
          "type": [
            "string",
            "null"
          ]
        },
        "fontFamily": {
          "description": "字体名称",
          "type": [
            "string",
            "null"
          ]
        },
        "fontSize": {
          "description": "字体大小，与坐标等单位相同",
          "type": [
            "number",
            "null"
          ]
        },
        "strikeout": {
          "description": "是否加删除线",
          "type": [
            "boolean",
            "null"
          ]
        },
        "underline": {
          "description": "是否加下划线",
          "type": [
            "boolean",
            "null"
          ]
        },
        "italic": {
          "description": "是否斜体",
          "type": [
            "boolean",
            "null"
          ]
        },
        "fontWeight": {
          "description": "是否加粗",
          "type": [
            "boolean",
            "null"
          ]
        },
        "align": {
          "description": "对齐模式\r\n\r\n 取值：\r\n - `LEFT_BOTTOM` 左底\r\n - `CENTER_BOTTOM` 中底\r\n - `RIGHT_BOTTOM` 右底\r\n - `LEFT_MIDDLE` 左中\r\n - `CENTER_MIDDLE` 中中\r\n - `RIGHT_MIDDLE` 右中\r\n - `LEFT_TOP` 左顶\r\n - `CENTER_TOP` 中顶\r\n - `RIGHT_TOP` 右顶",
          "type": "string",
          "enum": [
            "LEFT_BOTTOM",
            "CENTER_BOTTOM",
            "RIGHT_BOTTOM",
            "LEFT_MIDDLE",
            "CENTER_MIDDLE",
            "RIGHT_MIDDLE",
            "LEFT_TOP",
            "CENTER_TOP",
            "RIGHT_TOP"
          ],
          "enumDescriptions": {
            "LEFT_BOTTOM": "左底",
            "CENTER_BOTTOM": "中底",
            "RIGHT_BOTTOM": "右底",
            "LEFT_MIDDLE": "左中",
            "CENTER_MIDDLE": "中中",
            "RIGHT_MIDDLE": "右中",
            "LEFT_TOP": "左顶",
            "CENTER_TOP": "中顶",
            "RIGHT_TOP": "右顶"
          }
        }
      },
      "required": [
        "groupId",
        "locked",
        "zIndex",
        "x",
        "y",
        "rotation",
        "value",
        "color",
        "fillColor",
        "fontFamily",
        "fontSize",
        "strikeout",
        "underline",
        "italic",
        "fontWeight",
        "align"
      ]
    },
    "strokeColor": {
      "description": "描边颜色：`\"#RRGGBB\"` 十六进制色值；**null 表示采用主题默认色**\r\n\r\n 注：本文件各示例中的该字段**一律为 null**，非空色的具体串格式未在示例中出现；\r\n 可参照同文件 `TSchPin.color` 的 `@pattern ^$|^#[0-9A-Fa-f]{6}$`。",
      "type": [
        "string",
        "null"
      ]
    },
    "strokeStyle": {
      "description": "样式, null 为默认\r\n\r\n 取值：`SOLID` 实线、`SHORT_DASH` 短划线、`DOT` 点线、`DOT_DASH` 点划线。",
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "SOLID",
        "SHORT_DASH",
        "DOT",
        "DOT_DASH",
        null
      ],
      "enumDescriptions": {
        "SOLID": "实线",
        "SHORT_DASH": "短划线",
        "DOT": "点线",
        "DOT_DASH": "点划线"
      },
      "default": null
    },
    "fillColor": {
      "description": "填充颜色：`\"#RRGGBB\"` 十六进制色值；`\"\"` 表示不填充（填充会自动闭合起始点与结束点）；\r\n null 表示采用主题默认",
      "type": [
        "string",
        "null"
      ]
    },
    "strokeWidth": {
      "description": "宽度：null 表示采用主题默认线宽",
      "type": [
        "number",
        "null"
      ],
      "default": null
    },
    "fillStyle": {
      "description": "填充样式, null 为默认\r\n\r\n 取值：\r\n - `NONE` 无填充\r\n - `SOLID` 实心填充\r\n - `GRID` 网格\r\n - `HORIZONTAL_LINE` 横线\r\n - `VERTICAL_LINE` 竖线\r\n - `RHOMBIC` 菱形网格\r\n - `LEFT_SLASH_LINE` 左斜线\r\n - `RIGHT_SLASH_LINE` 右斜线",
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "NONE",
        "SOLID",
        "GRID",
        "HORIZONTAL_LINE",
        "VERTICAL_LINE",
        "RHOMBIC",
        "LEFT_SLASH_LINE",
        "RIGHT_SLASH_LINE",
        null
      ],
      "enumDescriptions": {
        "NONE": "无填充",
        "SOLID": "实心填充",
        "GRID": "网格",
        "HORIZONTAL_LINE": "横线",
        "VERTICAL_LINE": "竖线",
        "RHOMBIC": "菱形网格",
        "LEFT_SLASH_LINE": "左斜线",
        "RIGHT_SLASH_LINE": "右斜线"
      },
      "default": null
    }
  },
  "required": [
    "groupId",
    "locked",
    "zIndex",
    "centerX",
    "centerY",
    "radiusX",
    "radiusY",
    "rotation",
    "strokeColor",
    "strokeStyle",
    "fillColor",
    "strokeWidth",
    "fillStyle"
  ],
  "description": "椭圆"
}