{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "groupId": {
      "description": "分组编号：相同值的图元属于同一组；**未分组时的取值见下**。\r\n\r\n ⚠️ **未分组写的是数字 `0`**（不是字符串 `\"0\"`）——真实数据里 `\"groupId\":0` 出现\r\n 5 千余次，而 `\"groupId\":\"0\"` **一次都没有**；FOOTPRINT 文档下该字段**恒为 0**。\r\n 成组时才是字符串（组合 id）；未分组的判定是「`groupId` 为空或 FOOTPRINT 文档」，此时写**数字 0**。\r\n\r\n ⚠️ 上方的类型 `string` 比实际形态窄（装不下数字 0）——**放宽类型会波及 pro-* 仓的\r\n 消费方，需跨仓协调后再改**，故此处暂以说明为准。本文件各 `@example` 里的\r\n `\"groupId\":\"0\"` 也是同一问题，同理按“说明为准”处理。",
      "type": "string"
    },
    "locked": {
      "description": "是否锁定",
      "type": "boolean"
    },
    "zIndex": {
      "description": "同层内的叠放次序（渲染排序键，格式里没有高度语义）。\r\n\r\n **它是独立字段，不由 id 推导**：3.0 里未设置时写盘为 **-1**（内部实现即\r\n `_zIndex || -1`；真实数据 `\"zIndex\":-1` 出现 4 千余次）。**例外**：`ATTR` / `META`\r\n 的默认体直接写 `null`。\r\n\r\n 「由 id 尾部数字推导」是 **2.0 的语义**（2.0 的 `getZIndex` + `idReg`；那个正则也\r\n 匹配不了 3.0 的随机 16 位十六进制 id）。**不要照它推导 3.0 的值。**\r\n\r\n ⚠️ **VIA 是个特例：3.0 写盘不会产出该键**——写盘端的 switch 里**没有** zIndex 分支\r\n （虽然槽位表里有该槽位），取值为 `undefined` 时会被 JSON 序列化整个丢弃。\r\n 但**真机存量里仍能遇到 2.0 遗留的带 `zIndex` 的 VIA 行**（样本 15455 条 VIA 中\r\n 10404 条载荷含该键，其值恰好等于 id 尾部的数字——正是上面说的「2.0 语义」）。\r\n **读盘时不要假设它不存在。**",
      "type": [
        "number",
        "null"
      ],
      "default": -1
    },
    "partitionId": {
      "description": "所属分区编号，为null表示无分区",
      "type": [
        "string",
        "null"
      ]
    },
    "shellType": {
      "description": "外壳类型。\r\n\r\n 取值：`DRAWER` 推盖、`T&B` 上下壳。",
      "type": "string",
      "enum": [
        "DRAWER",
        "T&B"
      ],
      "enumDescriptions": {
        "DRAWER": "推盖",
        "T&B": "上下壳"
      }
    },
    "shellHeight": {
      "description": "外壳高度",
      "type": "number"
    },
    "pcbHeight": {
      "description": "PCB 高度",
      "type": "number"
    },
    "strokeWidth": {
      "description": "线宽（弃用）",
      "type": "number"
    },
    "path": {
      "description": "3D 外壳外轮廓（**复杂多边形**）：外层数组的每个元素是一条子路径。\r\n\r\n 【复杂多边形结构】首个子路径是**外轮廓**，其后的每个子路径都是**内洞**；\r\n 只有一条子路径时外层数组长度为 1。各子路径的方向按约定\r\n （外轮廓顺时针、内洞逆时针）书写。\r\n\r\n ⚠️ 实际读取时**只取首个子路径**（各解码器的 finish() 都会执行\r\n `path = path[0]`），后续内洞当前不会被使用。\r\n\r\n 【单多边形结构】每条子路径本身是扁平数组：前两个数字是起点坐标，其后是\r\n 「指令名 + 该指令参数」的重复序列——\r\n - `L` 直线：`x y L x y x y ...`，各点依次直线相连\r\n - `ARC` 圆弧：`startX startY ARC angle endX endY`（angle 逆时针为正、顺时针为负）\r\n - `CARC` 中心圆弧：`startX startY CARC angle endX endY`\r\n - `C` 三阶贝塞尔：`x1 y1 C x2 y2 x3 y3 x4 y4 ...`，四个一组为一条曲线\r\n\r\n 子路径也可整体写成与其它模式互斥的两种独立形态之一：\r\n - `[\"R\", x, y, width, height, rot, cornerRadius]` 矩形（x/y 为左上角）\r\n   —— 共 **7 个元素**，末位是**圆角半径**（不是 `isCCW`/`round`）。\r\n   编码为 `['R', x*10, y*10, w*10, h*10, rot°, radius*10]`；解码时 `cornerRadius = info[6] / 10`。\r\n - `[\"CIRCLE\", cx, cy, r]` 整圆",
      "type": "array",
      "items": {
        "type": "array"
      }
    },
    "thickness": {
      "description": "外壳厚度",
      "type": "number"
    },
    "direction": {
      "description": "推盖 - 推盖方向。\r\n\r\n 取值：`X_AXIS_POSITIVE` X轴正向、`X_AXIS_NEGATIVE` X轴负向、\r\n `Y_AXIS_POSITIVE` Y轴正向、`Y_AXIS_NEGATIVE` Y轴负向。",
      "type": "string",
      "enum": [
        "X_AXIS_POSITIVE",
        "X_AXIS_NEGATIVE",
        "Y_AXIS_POSITIVE",
        "Y_AXIS_NEGATIVE"
      ],
      "enumDescriptions": {
        "X_AXIS_POSITIVE": "X轴正向",
        "X_AXIS_NEGATIVE": "X轴负向",
        "Y_AXIS_POSITIVE": "Y轴正向",
        "Y_AXIS_NEGATIVE": "Y轴负向"
      }
    },
    "bottomHeight": {
      "description": "上下壳 - 下壳高度",
      "type": "number"
    },
    "topInnerHeight": {
      "description": "上下壳 - 上壳内壁高度",
      "type": "number"
    },
    "bottomInnerHeight": {
      "description": "上下壳 - 下壳内壁高度",
      "type": "number"
    }
  },
  "required": [
    "groupId",
    "locked",
    "zIndex",
    "shellType",
    "shellHeight",
    "pcbHeight",
    "strokeWidth",
    "path",
    "thickness"
  ],
  "description": "外壳"
}