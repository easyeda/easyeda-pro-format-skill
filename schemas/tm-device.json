{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "title": {
      "description": "名称",
      "type": "string"
    },
    "source": {
      "description": "来源：`<来源条目 uuid>|<来源工程 uuid>` 两段式（工程库独有）\r\n\r\n第 1 段是该条目在来源工程里的 uuid，第 2 段是**来源工程的 uuid**\r\n（真机形如 `\"2eb7b89d891135ec|bac6e37798284239bedae653a8874016\"`）。\r\n**工程内自建条目为空串**。\r\n\r\n读取端拿它去哪查：按 `|` 切开成 `[条目 uuid, 库标识]`，再用第 2 段当 **`path`（库标识）**\r\n去那个库里按第 1 段取条目 —— 第 2 段**不是文件路径**。",
      "type": "string"
    },
    "tags": {
      "description": "自定义分类",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "images": {
      "description": "图片链接",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "attributes": {
      "description": "属性",
      "type": "object"
    }
  },
  "required": [
    "title",
    "source",
    "tags",
    "images",
    "attributes"
  ],
  "description": "器件 META 类型\r\n\r\n器件文档（`DEVICE`）的 META 行，一行一条（`type:\"META\"`）：器件库条目的元数据——\r\n名称、分类标签、图片，以及一张**自由键值属性表** `attributes`（Value / Manufacturer 之类的设计属性都在这里）。\r\n在各类文档的 META 里，只有器件带这张自由属性表。\r\n\r\n器件文档的 META 行 id 是**固定单例名** `META`（属**固定单例 id**，见 `TSingletonElementId`）。"
}