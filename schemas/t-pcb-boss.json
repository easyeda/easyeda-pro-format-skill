{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "partitionId": {
      "description": "所属分区编号，为null表示无分区",
      "type": [
        "string",
        "null"
      ]
    },
    "groupId": {
      "description": "分组编号：相同值的图元属于同一组；**未分组时的取值见下**。\r\n\r\n ⚠️ **未分组写的是数字 `0`**（不是字符串 `\"0\"`）——真实数据里 `\"groupId\":0` 出现\r\n 5 千余次，而 `\"groupId\":\"0\"` **一次都没有**；FOOTPRINT 文档下该字段**恒为 0**。\r\n 成组时才是字符串（组合 id）；未分组的判定是「`groupId` 为空或 FOOTPRINT 文档」，此时写**数字 0**。\r\n\r\n ⚠️ 上方的类型 `string` 比实际形态窄（装不下数字 0）——**放宽类型会波及 pro-* 仓的\r\n 消费方，需跨仓协调后再改**，故此处暂以说明为准。本文件各 `@example` 里的\r\n `\"groupId\":\"0\"` 也是同一问题，同理按“说明为准”处理。",
      "type": "string"
    },
    "layerId": {
      "description": "层编号",
      "type": "number"
    },
    "locked": {
      "description": "是否锁定",
      "type": "boolean"
    },
    "zIndex": {
      "description": "同层内的叠放次序（渲染排序键，格式里没有高度语义）。\r\n\r\n **它是独立字段，不由 id 推导**：3.0 里未设置时写盘为 **-1**（内部实现即\r\n `_zIndex || -1`；真实数据 `\"zIndex\":-1` 出现 4 千余次）。**例外**：`ATTR` / `META`\r\n 的默认体直接写 `null`。\r\n\r\n 「由 id 尾部数字推导」是 **2.0 的语义**（2.0 的 `getZIndex` + `idReg`；那个正则也\r\n 匹配不了 3.0 的随机 16 位十六进制 id）。**不要照它推导 3.0 的值。**\r\n\r\n ⚠️ **VIA 是个特例：3.0 写盘不会产出该键**——写盘端的 switch 里**没有** zIndex 分支\r\n （虽然槽位表里有该槽位），取值为 `undefined` 时会被 JSON 序列化整个丢弃。\r\n 但**真机存量里仍能遇到 2.0 遗留的带 `zIndex` 的 VIA 行**（样本 15455 条 VIA 中\r\n 10404 条载荷含该键，其值恰好等于 id 尾部的数字——正是上面说的「2.0 语义」）。\r\n **读盘时不要假设它不存在。**",
      "type": [
        "number",
        "null"
      ],
      "default": -1
    },
    "centerX": {
      "description": "中心 X",
      "type": "number"
    },
    "centerY": {
      "description": "中心 Y",
      "type": "number"
    },
    "specification": {
      "description": "螺丝型号：\"M2\" \"M3\" 等，或者 null 表示自定义。\r\n\r\n 取值：`M2` M2 螺丝、`M3` M3 螺丝、`M4` M4 螺丝、`M5` M5 螺丝、`M6` M6 螺丝；\r\n `null` 表示自定义。",
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "M2",
        "M3",
        "M4",
        "M5",
        "M6",
        null
      ],
      "enumDescriptions": {
        "M2": "M2 螺丝",
        "M3": "M3 螺丝",
        "M4": "M4 螺丝",
        "M5": "M5 螺丝",
        "M6": "M6 螺丝"
      }
    },
    "depth": {
      "description": "螺丝柱高度",
      "type": "number"
    },
    "thickness": {
      "description": "螺丝柱壁厚",
      "type": "number"
    },
    "holeDiameter": {
      "description": "螺丝柱通孔直径",
      "type": "number"
    },
    "counterBore": {
      "description": "沉头所需参数 当此参数为 null 时表示不需要沉头",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "height": {
          "description": "沉头孔高度"
        },
        "diameter": {
          "description": "沉头孔直径"
        }
      }
    },
    "stiffener": {
      "description": "加强筋所需参数，当此参数为 null 时表示不需要加强筋",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "topWidth": {
          "description": "加强筋上端宽度"
        },
        "bottomWidth": {
          "description": "加强筋下端宽度"
        },
        "height": {
          "description": "加强筋高度"
        },
        "stiffThickness": {
          "description": "加强筋厚度"
        }
      }
    }
  },
  "required": [
    "groupId",
    "layerId",
    "locked",
    "zIndex",
    "centerX",
    "centerY",
    "specification",
    "depth",
    "thickness",
    "holeDiameter",
    "counterBore",
    "stiffener"
  ],
  "description": "螺丝柱"
}