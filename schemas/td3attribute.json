{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "materials": {
      "description": "基板材质",
      "type": "string"
    },
    "silkTechnology": {
      "description": "丝印工艺",
      "type": "string"
    },
    "backgroundColor": {
      "description": "背景颜色",
      "type": "string",
      "pattern": "^$|^#[0-9A-Fa-f]{6}$"
    },
    "boardColor": {
      "description": "板子颜色",
      "type": "string",
      "pattern": "^$|^#[0-9A-Fa-f]{6}$"
    },
    "sprayColor": {
      "description": "焊盘喷镀",
      "type": "string",
      "pattern": "^$|^#[0-9A-Fa-f]{6}$"
    },
    "layerExpose": {
      "description": "层发散",
      "type": "number"
    },
    "substrateHeight": {
      "description": "PCB 距外壳底面的高度：**单位为 mm**。\r\n\r\n⚠️ 读写两端都**不做 ×10 / ÷10 换算**（编码 `D3AttributeEncode` 原样返回、\r\n解码 `D3AttributeDecode` 直接取用），盘上的值就是 mm 值（例 `1.6` = 1.6 mm）。",
      "type": "number"
    }
  },
  "required": [
    "materials",
    "silkTechnology",
    "backgroundColor",
    "boardColor",
    "sprayColor",
    "layerExpose",
    "substrateHeight"
  ],
  "description": "3d 右侧面板属性\r\n\r\n一份文档里**只有一条**（`type:\"D3_ATTRIBUTE\"`），对应 **3D 视图右侧属性面板**上的那组\r\n外观设置：基板材质与丝印工艺，板子 / 焊盘喷镀 / 背景三种颜色，层发散，\r\n以及基板相对外壳底面的高度。\r\n\r\n⚠️ 它**不是图元**：没有层、没有坐标，也**不改变任何 2D 图元数据**，只影响 3D 侧的呈现。\r\n别与描述立体外壳几何的 `TPcbShell` 混为一谈 —— 一个是外观参数，一个是外壳轮廓。\r\n\r\nid 是**固定单例名** `\"D3_ATTRIBUTE\"`（这类**固定单例 id** 见 `TSingletonElementId`）。"
}