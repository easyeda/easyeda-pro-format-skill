{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "text": {
      "description": "被缓存的文字内容（**字体键第 1 槽**，共 8 槽）：写盘时进 id，盘上载荷不含本字段，\r\n 解析时由 id 反解填充",
      "type": "string"
    },
    "fontFamily": {
      "description": "字体名称（**字体键第 2 槽**，共 8 槽），如 `\"仿宋\"`、`\"default\"`",
      "type": "string"
    },
    "fontSize": {
      "description": "字号（**字体键第 3 槽**，共 8 槽；**内部单位 / 0.01 inch 口径**，与 width/height 不同）",
      "type": "number"
    },
    "thickness": {
      "description": "笔画粗细（**字体键第 4 槽**，共 8 槽）：生成时该槽写 10；\r\n ⚠️ **读取端不得依赖该槽恒为 10**——历史 / 异构数据里存在其它值（真机见过 `8`）",
      "type": "number"
    },
    "bold": {
      "description": "是否加粗（**字体键第 5 槽**，共 8 槽；写盘按 `bold?1:0` 取值）",
      "type": "boolean"
    },
    "italic": {
      "description": "是否斜体（**字体键第 6 槽**，共 8 槽；写盘按 `italic?1:0` 取值）",
      "type": "boolean"
    },
    "reverse": {
      "description": "是否反相扩展（**字体键第 7 槽**，共 8 槽；写盘按 `reverse?1:0` 取值）",
      "type": "boolean"
    },
    "expansion": {
      "description": "反相扩展尺寸（**字体键第 8 槽**，共 8 槽）：支持负数",
      "type": "number"
    },
    "width": {
      "description": "字形包围盒宽（**单位为 mil**——写盘时内部值 ×10，与 `path` 的口径不同）",
      "type": "number",
      "minimum": 0
    },
    "height": {
      "description": "字形包围盒高（**单位为 mil**，同 width）",
      "type": "number",
      "minimum": 0
    },
    "path": {
      "description": "字形轮廓（**复杂多边形**）：外层数组是**整段文本**的全部轮廓环，\r\n **不按字切分**——一个带内洞的字符本身就会占多个元素。\r\n 首环为外轮廓、后续各环为内洞。\r\n\r\n 每条子路径是扁平数组，前两个数字是起点坐标，其后是「指令名 + 该指令参数」的\r\n 重复序列（指令为绝对坐标指令，如 `L` 直线 / `C` 三阶贝塞尔）。\r\n\r\n 该字段是**字体缓存**：解析端可据此直接还原文字轮廓，无需加载字体文件。",
      "type": "array",
      "items": {
        "type": "array"
      }
    }
  },
  "required": [
    "width",
    "height",
    "path"
  ],
  "description": "字体缓存\r\n\r\n【id 构造】id **必须**是一个 **8 槽 JSON 数组**，缺一不可：\r\n`[text, fontFamily, fontSize, thickness, bold?1:0, italic?1:0, reverse?1:0, expansion]`\r\n（见 `getFontId`）；其中**第 4 槽 `thickness` 生成时写 10**。\r\n真实 id 形如 `[\"████ 8055YP_LHB_1\",\"Arial\",59.05512,10,0,0,0,0]`。\r\n⚠️ 该槽**读取端不得依赖恒为 10**：历史 / 异构数据里存在其它值\r\n（真机同一份 FONT 文档里就有 `8`，如 `[\"+\",\"default\",3.937,8,0,0,0,0]`）。\r\n\r\n【落盘形态】写盘端**只写 `width` / `height` / `path` 三个字段**；\r\n其余 8 个是**字体键的槽位**，解析时**由 id 反解填充**\r\n（`parseFontId(id)` + `Object.assign(itemObj, data)`）。\r\n\r\n【长度单位不统一，按字段看】本类型的字段口径**互不相同**：\r\n- `width` / `height` —— **mil**（写盘时内部值 ×10）；\r\n- `fontSize` —— **内部单位 / 0.01 inch 口径**（1 单位 = 10 mil = 0.254 mm）。\r\n  **与文字元素的关系**：`= 该文字元素格式 `fontSize`（STRING/ATTR/DIMENSION，mil）的 1/10`。\r\n  写盘端把内部渲染值（px）原样透传到槽 3，而文字元素在写盘时把内部值 ×10 进格式，\r\n  所以槽 3 数值 = 文字元素格式 fontSize ÷ 10（两侧各自自洽，读回也是这个值，别重复换算）。\r\n- `path` —— **原样搬运**，不做换算。\r\n\r\n【行数据外壳】每条数据是一行，形如 `{\"type\":..., \"id\":..., \"ticket\":...}||{...}`：\r\n前半段是通用外壳（`type` 取数据流里的类型取值，`ticket` 是逻辑时钟），后半段才是本类型描述的内容；\r\n外壳还可能带 `client` / `firstTicket` / `pend` / `num` 等可选键（**真机 FONT 行就带 `client`**），\r\n读取端**透传即可**。**行终止符是 `|` 加换行（LF，不是 CRLF）**：`||` 是外壳/载荷分界，\r\n行尾的 `|`+换行才是记录结束。"
}