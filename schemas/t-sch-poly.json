{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "partId": {
      "description": "所属部件编号（**可选**，真机存在整键缺失的 COMPONENT）：\r\n**符号页**上表示自身归属于哪个部件，**图页**的元件则指向它**关联的符号文档**里的部件。\r\n取值即目标 `PART` 行的 `id`（`PART` 行只存在于 SYMBOL 文档，见 `TPart`）。",
      "type": "string"
    },
    "groupId": {
      "description": "分组编号；**两种「未成组」形态都表示未成组**：\r\n- **3.0 写盘时该键根本不存在**：写入端取 `model.combination?.id`，未成组即 `undefined`，\r\n  经 `JSON.stringify` 后整键被丢弃，消费方须**按可选键处理**；\r\n- **`\"\"` 只出现在 2.0 迁移数据里**（2.0 → 3.0 转换腿原样带着它）。",
      "type": "string"
    },
    "locked": {
      "description": "是否锁定。\r\n\r\n⚠️ **原理图侧目前只是数据字段**：pro-sch 里没有任何按它拦截拖拽 / 删除 / 改大小 /\r\n改形状的实现（连「锁定 / 解锁」命令本身都是空实现，只 publish 属性面板刷新）。\r\n那四条行为特征只在 **PCB / 面板**侧落实。生成数据时照常写入本字段，\r\n但**不要指望原理图端会因为它而禁止操作**。",
      "type": "boolean"
    },
    "zIndex": {
      "description": "同层内的叠放次序：渲染排序键，数值越小越靠下（格式里没有高度语义）",
      "type": [
        "number",
        "null"
      ]
    },
    "yAxisDirection": {
      "description": "Y 轴方向标记：**仅 eprj3 本地文件格式会带**，读盘时被剥离。\r\n\r\n取值：`up` = **笛卡尔坐标系（Y 向上）**，本行坐标已按它写出（本地文件固定用它），`down` = **屏幕坐标系（Y 向下）**（编辑器内部与云端的常态）\r\n——**字段缺失等价于它**，两者是同一件事；两套坐标系下同一个形状的 y **互为相反数**。完整语义见 `TYAxisDirection`。\r\n\r\n⚠️ 本基类被多个图元继承，**各自被翻转的字段不同**——完整对照表见 `TYAxisDirection`。\r\n本基类直接覆盖的：`ATTR`/`TEXT`/`PIN`/`COMPONENT` 翻 `y`；`OBJ`/`TABLE` 翻 `startY`；\r\n`ARC` 翻 `startY`/`referY`/`endY`；`CIRCLE`/`ELLIPSE` 翻 `centerY` + 内嵌 `text.y`；\r\n`RECT` 翻 `dotY1`/`dotY2` + `text.y`；`MASK_REGION` 翻 `dotY1`/`dotY2`；`POLY` 翻\r\n`points` 各项的 `y`；`BEZIER` 翻 `controls` 的奇数下标。\r\n`rotation` / `isMirror` / `zIndex` 与所有幅值字段**不翻**。",
      "type": "string",
      "enum": [
        "up",
        "down"
      ]
    },
    "points": {
      "description": "点集坐标",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "x": {
            "description": "坐标x轴数值（0.01 inch）",
            "type": "number"
          },
          "y": {
            "description": "坐标y轴数值（0.01 inch）",
            "type": "number"
          }
        },
        "required": [
          "x",
          "y"
        ],
        "description": "坐标点"
      }
    },
    "closed": {
      "description": "是否自动闭合：如果自动闭合，则结束点会自动连上起始点",
      "type": "boolean"
    },
    "startShape": {
      "description": "起点形状：3.3 新增；**解码时若字段缺失会被补 `null`**",
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "NONE",
        "ARROW",
        "SOLID_ARROW",
        "TAIL",
        "SOLID_TAIL",
        "CIRCLE",
        "SQUARE",
        "RHOMBUS",
        null
      ],
      "enumDescriptions": {
        "NONE": "无",
        "ARROW": "箭头",
        "SOLID_ARROW": "实心箭头",
        "TAIL": "尾巴",
        "SOLID_TAIL": "燕尾箭头",
        "CIRCLE": "圆形",
        "SQUARE": "方形",
        "RHOMBUS": "菱形"
      },
      "default": null
    },
    "endShape": {
      "description": "终点形状：3.3 新增；**解码时若字段缺失会被补 `null`**",
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "NONE",
        "ARROW",
        "SOLID_ARROW",
        "TAIL",
        "SOLID_TAIL",
        "CIRCLE",
        "SQUARE",
        "RHOMBUS",
        null
      ],
      "enumDescriptions": {
        "NONE": "无",
        "ARROW": "箭头",
        "SOLID_ARROW": "实心箭头",
        "TAIL": "尾巴",
        "SOLID_TAIL": "燕尾箭头",
        "CIRCLE": "圆形",
        "SQUARE": "方形",
        "RHOMBUS": "菱形"
      },
      "default": null
    },
    "strokeColor": {
      "description": "描边颜色：`\"#RRGGBB\"` 十六进制色值；**null 表示采用主题默认色**\r\n\r\n注：本文件各示例中的该字段**一律为 null**，非空色的具体串格式未在示例中出现；\r\n可参照同文件 `TSchPin.color` 的 `@pattern ^$|^#[0-9A-Fa-f]{6}$`。",
      "type": [
        "string",
        "null"
      ]
    },
    "strokeStyle": {
      "description": "样式, null 为默认",
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "SOLID",
        "SHORT_DASH",
        "DOT",
        "DOT_DASH",
        null
      ],
      "enumDescriptions": {
        "SOLID": "实线",
        "SHORT_DASH": "短划线",
        "DOT": "点线",
        "DOT_DASH": "点划线"
      },
      "default": null
    },
    "fillColor": {
      "description": "填充颜色：`\"#RRGGBB\"` 十六进制色值；`\"\"` 表示不填充（填充会自动闭合起始点与结束点）；\r\nnull 表示采用主题默认",
      "type": [
        "string",
        "null"
      ]
    },
    "strokeWidth": {
      "description": "宽度：null 表示采用主题默认线宽\r\n单位：**0.01 inch**",
      "type": [
        "number",
        "null"
      ],
      "default": null
    },
    "fillStyle": {
      "description": "填充样式, null 为默认",
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "NONE",
        "SOLID",
        "GRID",
        "HORIZONTAL_LINE",
        "VERTICAL_LINE",
        "RHOMBIC",
        "LEFT_SLASH_LINE",
        "RIGHT_SLASH_LINE",
        null
      ],
      "enumDescriptions": {
        "NONE": "无填充",
        "SOLID": "实心填充",
        "GRID": "网格",
        "HORIZONTAL_LINE": "横线",
        "VERTICAL_LINE": "竖线",
        "RHOMBIC": "菱形网格",
        "LEFT_SLASH_LINE": "左斜线",
        "RIGHT_SLASH_LINE": "右斜线"
      },
      "default": null
    }
  },
  "required": [
    "groupId",
    "locked",
    "zIndex",
    "points",
    "closed",
    "startShape",
    "endShape",
    "strokeColor",
    "strokeStyle",
    "fillColor",
    "strokeWidth",
    "fillStyle"
  ],
  "description": "原理图多边形\r\n\r\n**原理图族里的折线 / 多边形**，一行一条（`type:\"POLY\"`）：由 `points` 顺序相连成一张图形，\r\n`closed` 为真时把**结束点自动连回起始点**（闭合之后填充才围得住区域）。\r\n\r\n`startShape` / `endShape`（3.3 新增）给两端加箭头 / 燕尾等端头，解码时字段缺失会被补 `null`。\r\n\r\n⚠️ **同名不同义**：封装域也有一个 `POLY`，但那边存的是路径串而不是点数组，\r\n字段与取值都不通用，别照另一套写。\r\n\r\n本行的 `id` 由编辑器生成（随机 16 位小写十六进制），图元 id 的几类形态与判别见 `TElementId`。"
}