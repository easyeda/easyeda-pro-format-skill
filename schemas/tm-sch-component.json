{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "groupId": {
      "description": "分组编号；**两种「未成组」形态都表示未成组**：\r\n - **3.0 写盘时该键根本不存在**：写入端取 `model.combination?.id`，未成组即 `undefined`，\r\n   经 `JSON.stringify` 后整键被丢弃，消费方须**按可选键处理**；\r\n - **`\"\"` 只出现在 2.0 迁移数据里**（2.0 → 3.0 转换腿原样带着它）。",
      "type": "string"
    },
    "locked": {
      "description": "是否锁定",
      "type": "boolean"
    },
    "zIndex": {
      "description": "同层内的叠放次序：渲染排序键，数值越小越靠下（格式里没有高度语义）",
      "type": [
        "number",
        "null"
      ]
    },
    "yAxisDirection": {
      "description": "Y 轴方向标记：**仅 eprj3 本地文件格式会带**，读盘时被剥离。\r\n\r\n 取值：`up` **已按 Y 轴向上写出**（本地文件固定用它）、`down` Y 轴向下\r\n （**字段缺失等价于它**，两者是同一件事）。完整语义见 `TYAxisDirection`。\r\n\r\n ⚠️ 本基类被多个图元继承，**各自被翻转的字段不同**——完整对照表见 `TYAxisDirection`。\r\n 本基类直接覆盖的：`ATTR`/`TEXT`/`PIN`/`COMPONENT` 翻 `y`；`OBJ`/`TABLE` 翻 `startY`；\r\n `ARC` 翻 `startY`/`referY`/`endY`；`CIRCLE`/`ELLIPSE` 翻 `centerY` + 内嵌 `text.y`；\r\n `RECT` 翻 `dotY1`/`dotY2` + `text.y`；`MASK_REGION` 翻 `dotY1`/`dotY2`；`POLY` 翻\r\n `points` 各项的 `y`；`BEZIER` 翻 `controls` 的奇数下标。\r\n `rotation` / `isMirror` / `zIndex` 与所有幅值字段**不翻**。",
      "type": "string",
      "enum": [
        "up",
        "down"
      ]
    },
    "partId": {
      "description": "所属部件编号：指向**关联符号文档**里的 `PART` 行 id（`PART` 行只存在于 SYMBOL 文档，\r\n 见 `TPart` 的说明）——取值就是那个符号文档里 `key:\"Symbol\"` 属性值所标识文档中的 PART 行。\r\n\r\n ⚠️ 旧注释说「符号页专属 / 默认 `\"\"`」不准确：**图页元件也用本字段**（真机 SCH 文档的\r\n COMPONENT 带 `partId:\"-1\"`）。协议类型上它是必填，但**真机存在整键缺失的 COMPONENT**，\r\n 读取时按可空处理。",
      "type": "string"
    },
    "x": {
      "description": "位置 X",
      "type": "number"
    },
    "y": {
      "description": "位置 Y",
      "type": "number"
    },
    "rotation": {
      "description": "旋转角度（角度制）：绕 位置 旋转",
      "type": "number",
      "minimum": 0,
      "maximum": 360
    },
    "isMirror": {
      "description": "是否镜像",
      "type": "boolean"
    },
    "attrs": {
      "description": "自定义属性",
      "type": "object",
      "properties": {
        "DeviceName": {
          "description": "当前绑定文本器件（uuid 可空），v4 新增; `{ uuid: string; name: string; source: string }`的JSON.string序列化"
        },
        "Devices": {
          "description": "备选器件，v4 新增;`{ uuid: string; name: string; source: string }[]`的JSON.string序列化"
        },
        "FootprintName": {
          "description": "当前绑定文本封装（uuid 可空），v4 新增; `{ uuid: string; name: string; source: string }`的JSON.string序列化"
        },
        "Footprints": {
          "description": "备选封装，v4 新增;`{ uuid: string; name: string; source: string }[]`的JSON.string序列化"
        },
        "SymbolName": {
          "description": "当前绑定文本符号（uuid 可空），v4 新增; `{ uuid: string; name: string; source: string }`的JSON.string序列化"
        },
        "Symbols": {
          "description": "备选符号，v4 新增;`{ uuid: string; name: string; source: string }[]`的JSON.string序列化"
        },
        "[key: string]": {
          "description": "其他自定义"
        }
      }
    }
  },
  "required": [
    "groupId",
    "locked",
    "zIndex",
    "partId",
    "x",
    "y",
    "rotation",
    "isMirror",
    "attrs"
  ],
  "description": "元件（COMPONENT 图元的**线格式**）\r\n\r\n**本类型描述的就是盘上的形状**：`attrs` 里的 6 个固定键（`DeviceName` / `Devices` /\r\n`SymbolName` / `Symbols` / `FootprintName` / `Footprints`）都是**逐键 JSON 字符串**。\r\n解码后 `attrs` 会被还原成对象，那个形态是 `TSchComponent`——\r\n**生成格式数据时用本类型**，不要用解码后的那个。\r\n\r\n【联动增删·必须成组操作】COMPONENT 与其 ATTR 是**同一生命周期**：\r\n**新增 COMPONENT 时必须一并写出它的 ATTR；删除 COMPONENT 时必须一并删除它的 ATTR**，\r\n只删 COMPONENT 会留下悬空引用。（PCB 侧同构，另有 PAD_NET 一并处理。）"
}