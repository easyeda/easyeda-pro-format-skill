{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "partitionId": {
      "description": "所属分区编号，为null表示无分区",
      "type": [
        "string",
        "null"
      ]
    },
    "groupId": {
      "description": "分组编号：相同值的图元属于同一组；**未分组时的取值见下**。\r\n\r\n ⚠️ **未分组写的是数字 `0`**（不是字符串 `\"0\"`）——真实数据里 `\"groupId\":0` 出现\r\n 5 千余次，而 `\"groupId\":\"0\"` **一次都没有**；FOOTPRINT 文档下该字段**恒为 0**。\r\n 成组时才是字符串（组合 id）；未分组的判定是「`groupId` 为空或 FOOTPRINT 文档」，此时写**数字 0**。\r\n\r\n ⚠️ 上方的类型 `string` 比实际形态窄（装不下数字 0）——**放宽类型会波及 pro-* 仓的\r\n 消费方，需跨仓协调后再改**，故此处暂以说明为准。本文件各 `@example` 里的\r\n `\"groupId\":\"0\"` 也是同一问题，同理按“说明为准”处理。",
      "type": "string"
    },
    "layerId": {
      "description": "层编号",
      "type": "number"
    },
    "locked": {
      "description": "是否锁定",
      "type": "boolean"
    },
    "zIndex": {
      "description": "同层内的叠放次序（渲染排序键，格式里没有高度语义）。\r\n\r\n **它是独立字段，不由 id 推导**：3.0 里未设置时写盘为 **-1**（内部实现即\r\n `_zIndex || -1`；真实数据 `\"zIndex\":-1` 出现 4 千余次）。**例外**：`ATTR` / `META`\r\n 的默认体直接写 `null`。\r\n\r\n 「由 id 尾部数字推导」是 **2.0 的语义**（2.0 的 `getZIndex` + `idReg`；那个正则也\r\n 匹配不了 3.0 的随机 16 位十六进制 id）。**不要照它推导 3.0 的值。**\r\n\r\n ⚠️ **VIA 是个特例：3.0 写盘不会产出该键**——写盘端的 switch 里**没有** zIndex 分支\r\n （虽然槽位表里有该槽位），取值为 `undefined` 时会被 JSON 序列化整个丢弃。\r\n 但**真机存量里仍能遇到 2.0 遗留的带 `zIndex` 的 VIA 行**（样本 15455 条 VIA 中\r\n 10404 条载荷含该键，其值恰好等于 id 尾部的数字——正是上面说的「2.0 语义」）。\r\n **读盘时不要假设它不存在。**",
      "type": [
        "number",
        "null"
      ],
      "default": -1
    }
  },
  "required": [
    "groupId",
    "layerId",
    "locked",
    "zIndex"
  ],
  "description": "基础字段（PCB 系列所有图元的公共前缀）\r\n\r\n【通用约定】下列约定适用于本文件（PCB / FOOTPRINT）**全部**以本类型为基础的图元：\r\n- **坐标与长度的单位是 1 mil**（1 单位 = 0.001 inch = 0.0254 mm）。\r\n  例如 `\"width\": 10` 就是 10 mil（= 0.254 mm，常见的默认走线宽）。\r\n  注意：编辑器**内部**用的是「像素」（1 像素 = 10 mil），写盘时 ×10、读盘时 ÷10，\r\n  所以格式里看到的值比内部值大 10 倍，**别把内部值直接当格式值写**。\r\n  （与原理图/面板不同——那两边的格式值单位是 0.01 inch，见各自文件的说明。）\r\n- **角度单位是度**（编辑器内部按弧度使用，读写时需换算）。\r\n- 每条数据是一行，形如 `{\"type\":..., \"id\":..., \"ticket\":...}||{...}`：前半段是通用外壳\r\n  （`type` 取 `EPcbDataType` 的值，`ticket` 是逻辑时钟），后半段才是各类型描述的内容。\r\n  行终止符是 `|` 加换行（**LF，不是 CRLF**）：`||` 是外壳/载荷分界，行尾的 `|`+换行才是记录结束。\r\n- **`id` 分两类**：\r\n  - 多数图元是**随机 16 位小写十六进制**字符串；\r\n  - 少数单例/集合型图元用**有含义的固定 id**，且多为 **JSON 数组串**形式，\r\n    如 `[\"LAYER\", 层号]`、`[\"RULE\", 规则类型, 规则名]`、`[\"NET\", 网络名]`、\r\n    `[\"X_NET_GROUP\", 组名]`、`[\"POURED\", 覆铜边框 id]`、`[\"PRIMITIVE\", 名称, 层号?]`。"
}